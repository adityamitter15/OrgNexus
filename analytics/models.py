# File: analytics/models.py - Aditya Mitter (W19869650)
# Analytics is a read-only app on top of teams + reports + organisation,
# so it has no models of its own. Charts read from those existing tables.
